import 'package:flutter/widgets.dart';

class CreateNoticeFailureWidget extends StatelessWidget {
  const CreateNoticeFailureWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Błąd dodawania ogłoszenia!"),
    );
  }
}
